// import { exposeEventListeners, hide, show, gridShow, find } from "UtilFunctions";


async function compareGivenUsername(event){

    let username = event.value;
    let result = await compareDetail(username);
    
    // update the UI to show the user if the name is available

}